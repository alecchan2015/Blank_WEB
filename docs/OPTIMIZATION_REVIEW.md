# 代码优化 Review 报告

> **最近更新**: 2026-04-19
> **更新频率**: 每日上午 07:07 自动刷新
> **状态**: 📋 待用户确认后执行

---

## 💸 Token 消费优化（按节省量排序）

| # | 位置 | 问题 | 预估节省 | 改动量 |
|---|------|------|---------|--------|
| **1** | `services/agent_service.py:100-103` | **KB 知识注入不做相关性筛选**，按默认顺序取最新 5 条塞进 prompt（每次~2000 tokens），3 个专家 = ~6000 tokens | **30-50%** system prompt | 中 |
| **2** | `services/agent_service.py:162-168` | **上游 agent 输出完整透传**（strategy + brand 输出各 2-4K tokens），operations 上下文可膨胀到 4-8K tokens | **20-35%** (operations 调用) | 小 |
| **3** | `services/pptx_agent.py:36-155` | **SLIDE_SCHEMA 有 ~1100 tokens 的装饰性注释**（`// ① Cover`、`// ⑫ 2×2 Matrix` 等），LLM 完全不需要 | **35%** schema prompt | 零风险 |
| **4** | `services/seed_knowledge.py:920-957` | **战略 prompt 里框架列表重复 2 次**（Instructions 第5-9条和"知识框架"都列了 Porter/BCG/蓝海等） | ~120 tokens/次 | 5 分钟 |
| **5** | `services/kb_upload_service.py:150` | KB 上传 **分块粒度 1000 字符过小**，一个 5K 文档触发 6 次 LLM 调用，还不做重复去重 | **15-25%** 上传成本 | 中 |
| **6** | `services/llm_service.py:111,162` | `max_tokens=4096` 所有调用一刀切，KB 提取其实 1K 就够 | 辅助节流 | 小 |

---

## ⚡ 性能优化（按用户感知排序）

| # | 位置 | 问题 | 预估提升 | 改动量 |
|---|------|------|---------|--------|
| **1** | `frontend/src/main.js:5,31-33` | **整个 Element Plus 图标库全量注册**（~300 个图标，实际用 <10 个），导致主包 **1.2MB**（gzip 391KB） | **首屏 -2~4 秒**（16M 带宽） | 小 |
| **2** | `backend/main.py:692-717` | **任务详情 N+1 查询**，每个 agent 跑 2 次查询，3 agents = 6 次 | 每次 -20~40ms | 小 |
| **3** | `backend/main.py:2753-2789` + `3164-3192` | **Logo/海报进度 SSE 每 3 秒开新 SQLite session**（最多各 120 次 = 10 分钟），与后台生成线程抢锁；海报为新增功能，引入同一模式 | 消除卡顿风险 | 中 |
| **4** | `backend/models.py:152-175` | **`Task.user_id`/`Task.status`/`TaskResult.task_id`/`AgentKnowledge.agent_type` 无索引**，全表扫描；其中 `AgentKnowledge.agent_type` 每次 agent 调用都查询 | 数据多了 10-50ms/查询 | 小 |
| **5** | `backend/services/llm_service.py:202-216` | **`get_llm_config_for_agent()` 每次 agent 调用都查库**，3 agent = 3-6 次重复查询 | 每任务 -15ms | 小 |
| **6** | `backend/services/ppt_settings.py:36` + `logo_settings.py:51` + `poster_settings.py:49` | **每次生成 PPT/Logo/海报都 `load_config()` 查库**，但配置几乎不变；共 3 个设置文件有此问题 | 每次 -5ms | 小 |
| **7** | `backend/main.py:658-673` | **`GET /api/tasks` 列表把 results 全部带回**，前端只用 `results.length` | 载荷减 100-500KB | 中 |
| **8** | `backend/services/file_service.py` + `pptx_service.py` | **regex 每次调用都重新编译** | 微优化 | 零风险 |

---

## 🎯 推荐执行顺序

### 第一批（高价值低风险，约 1 小时内完成）
1. **图标按需导入** — 首屏快 2-4 秒
2. **SLIDE_SCHEMA 去装饰注释** — PPT 生成省 35% token
3. **添加 DB 索引** — `Task.user_id`/`Task.status`/`TaskResult.task_id`/`AgentKnowledge.agent_type`
4. **LLM/PPT/Logo/海报配置缓存** — 4 个文件各加 module-level dict
5. **去重复的"知识框架"段** — 5 分钟
6. **任务详情 N+1 查询合并** — 改成一次查询再分组

**预估合计**：token 节省 ~40%，首屏快 2-4 秒

### 第二批（需要仔细设计）
7. **KB 相关性筛选**（关键词匹配/TF-IDF，不需要向量库）
8. **上游 agent 输出自动总结**（300-500 token 摘要）
9. **KB 上传分块调大到 2000 + 去重**

### 第三批（改动面大）
10. **Logo/海报 SSE 改短轮询或回调通知**（消除 SQLite 并发锁风险）
11. **任务列表接口加 `result_count` 字段**
12. **`max_tokens` 按场景分级**

---

## 🔧 执行方式

用户确认具体批次后，告诉 Claude:
> 执行第一批优化 / 执行 #1 #2 #3 / 全部执行

Claude 会按顺序改动、测试、提交并部署到云服务器。

---

## 📌 附录 · Review 方法论

本报告由 Claude 扫描以下文件自动生成：
- 后端：`backend/main.py`、`backend/services/*.py`、`backend/models.py`
- 前端：`frontend/src/views/*.vue`、`frontend/src/main.js`、`frontend/vite.config.js`

检查维度：
- Token 消费：prompt 膨胀、上下文累积、重复调用、非必要 LLM 调用
- 性能：N+1 查询、同步阻塞、索引缺失、缓存缺失、前端包体积
- 基础设施：已排除（当前为 16Mbps 带宽、SQLite、4 核）

> 🤖 Generated by [Claude Code](https://claude.com/claude-code)

---

## 📜 变更日志

### 2026-04-19

**新增**
- ⚡ 性能 #3 更新：海报进度 SSE（`main.py:3164-3192`）与 Logo SSE 采用相同的新 session/poll 模式，是 44874c1 海报功能引入的新问题，纳入同一条目
- ⚡ 性能 #4 更新：新增 `AgentKnowledge.agent_type` 缺失索引——该列在每次 agent 调用时都被 filter，但没有 `index=True`
- ⚡ 性能 #6 更新：`poster_settings.py` 新增后同样缺少模块级缓存，条目从 2 个文件扩展到 3 个文件

**修复**
- 本周期无已修复条目（所有既有问题仍待处理）
